/**
 * admin.ts
 * 이 파일은 더 이상 Prisma DB를 사용하지 않습니다.
 * 모든 데이터(학생 명단, 교사, 장소, 호출 기록)는
 * 브라우저 localStorage(localStore.ts)에서 관리됩니다.
 *
 * 서버 역할: Socket.IO 실시간 중계만 수행
 */

// 빈 export — 파일을 삭제하지 않고 유지하여 빌드 캐시 안정성 확보
export {};
